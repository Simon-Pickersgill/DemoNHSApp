import { PatientSearchParams } from './patient-search-params';

describe('PatientSearchParams', () => {
  it('should create an instance', () => {
    expect(new PatientSearchParams()).toBeTruthy();
  });
});
